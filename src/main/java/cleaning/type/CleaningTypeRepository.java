package cleaning.type;

import cleaning.type.CleaningType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CleaningTypeRepository extends JpaRepository<CleaningType, Integer> {
}
